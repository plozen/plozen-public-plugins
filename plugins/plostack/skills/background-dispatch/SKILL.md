---
name: background-dispatch
description: 사용자가 `-bd`, background dispatch, 백그라운드 위임, subagent 병렬 작업, 스폰 후 계속 대화를 명시하거나 경량/표준/보호 분류 후 독립 실행 단위, 역할 분리, 장시간 작업 조건 때문에 background subagent 호출 여부를 판단해야 할 때 사용한다. 스폰 직후 메인 대화를 오래 점유하지 않고, 명시적 취소 전까지 백그라운드 작업을 유지하며, 결과가 돌아오면 통합한다.
---

# Background Dispatch Skill

이 스킬은 background subagent를 띄운 뒤 메인 에이전트가 사용자와 계속 대화할 수 있게 하는 운영 규칙이다. 구현/조사 자체를 대신하는 스킬이 아니라, `spawn_agent` 이후의 대화 양보와 lifecycle 관리를 강제한다.

## 사용 시점

다음 중 하나라도 있으면 사용한다.

- 사용자 입력 끝에 `-bd`가 있다.
- 사용자가 "백그라운드로", "background dispatch", "서브에이전트로 돌리고 계속하자", "병렬로 맡겨"라고 말한다.
- 경량/표준/보호 분류 후 독립 실행 단위, 역할 분리, 장시간 작업 조건 때문에 background subagent 호출 여부를 판단해야 한다.
- 표준/보호 작업에서 장시간 조사, QA, 리뷰, 보안 점검, 구현 단위 분리가 필요하다.
- subagent를 이미 스폰했고, 그 작업을 기다릴지 계속 대화할지 판단해야 한다.

## 핵심 규칙

1. `spawn_agent` 호출은 메인 대화에서 한 번 실행하지만, 스폰된 작업은 백그라운드로 취급한다.
2. 스폰 직후 `wait_agent`를 기본 호출하지 않는다. 결과가 즉시 critical path를 막을 때만 기다린다.
3. 스폰 직후 긴 웹검색, 중복 조사, 대량 파일 읽기 같은 foreground 작업을 이어가지 않는다. 먼저 짧게 디스패치 사실을 보고하고 사용자 입력을 받을 수 있게 한다.
4. 사용자가 중간에 다른 질문을 해도, 새 요청이 기존 background 작업과 충돌하지 않으면 subagent를 닫지 않는다.
5. 사용자가 `Esc`, turn abort, 새 질문, 상태 질문을 보냈다는 이유만으로 취소 의도로 해석하지 않는다.
6. `close_agent`는 사용자가 명시적으로 "중단", "취소", "닫아", "그 작업 버려"라고 했거나, 작업이 완료되어 결과 통합이 끝났을 때만 호출한다.
7. background 결과가 도착하면 메인 에이전트가 검토, 저장, 카드 반영, 최종 보고를 맡는다. 같은 일을 처음부터 다시 하지 않는다.

## Dispatch 절차

### 1. 분해

스폰 전 작업을 즉시 실행 단위와 백그라운드 단위로 나눈다.

- 즉시 실행 단위: 사용자와 지금 정해야 하는 판단, 카드 구조, 승인, 범위 정리.
- 백그라운드 단위: 장시간 조사, 독립 구현, QA, 리뷰, 보안 점검, 브라우저 검증.

즉시 다음 행동이 백그라운드 결과 없이는 불가능하면 foreground로 처리하거나, 스폰 후 `wait_agent`가 필요한 이유를 짧게 말한다.

### 2. 스폰

subagent 프롬프트에는 아래를 반드시 넣는다.

- 미션 1줄
- 작업 범위와 금지 범위
- 수정 권한 여부
- 산출물 형식
- 시간 한도 또는 종료 기준
- 파일 소유권, 수정 가능 경로

코드 수정 작업이면 "다른 작업자가 있을 수 있으니 타인의 변경을 되돌리지 말라"를 포함한다.

### 3. 양보

스폰 직후 메인 응답은 짧게 끝낸다.

```text
백그라운드 디스패치 완료: {agent/nickname}에게 {작업}을 맡겼습니다.
저는 여기서 기다리지 않고, 지금은 {메인에서 계속할 작업/질문}으로 이어가겠습니다.
```

스폰 직후 같은 주제의 긴 foreground 조사를 계속하지 않는다. 필요한 경우에도 사용자 입력을 받을 수 있는 짧은 단위로만 진행한다.

### 4. 진행 중 사용자 입력 처리

사용자 입력을 아래처럼 해석한다.

- 상태 질문: 현재 알고 있는 agent 상태를 답하고 계속 둔다.
- 관련 구조 질문: 메인 에이전트가 답하되, background 작업은 유지한다.
- 새 작업 요청: 충돌하지 않으면 병행한다.
- 명시적 취소: 해당 agent를 닫고 취소 사실을 보고한다.
- 충돌 요청: 어느 작업을 우선할지 짧게 확인하거나, 충돌하지 않는 최소 조치만 한다.

## 금지

- `-bd` 요청 후 바로 `wait_agent`로 장시간 막기.
- 사용자가 새 질문을 했다는 이유만으로 background agent를 종료하기.
- 메인 에이전트가 background subagent와 같은 조사를 중복 수행하기.
- background 결과를 받기 전에 성공/완료로 보고하기.
- agent id를 잃어버렸는데 완료됐다고 추정하기.

## 완료 처리

background 작업이 끝나면 다음 순서로 통합한다.

1. 결과를 읽고 요구 산출물이 있는지 확인한다.
2. 필요한 경우 파일 diff, 출처, 테스트 결과를 검증한다.
3. Vault/Todo/문서/코드에 반영할 항목만 반영한다.
4. 완료 보고에는 `디스패치한 것`, `통합한 것`, `검증한 것`, `남은 것`을 짧게 남긴다.

## 실패/중단 처리

- subagent가 실패하면 실패 이유와 unblock path를 보고한다.
- 사용자가 turn을 끊었거나 세션이 재개됐으면, 이전 background 작업을 취소로 추정하지 않는다.
- 현재 런타임에서 agent 상태를 확인할 수 없으면 "상태 확인 불가"라고 말하고, 임의로 완료/취소를 선언하지 않는다.
